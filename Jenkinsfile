pipeline {
    agent any

    stages {
        stage ('Compile Stage') {

            steps {
           
                    sh ' compile stage ' 
                    echo "hello world"
                }
            }
        }
}
